function canPlay() {
  let sport = " Football";

  if (true) {
    let personName = "Cosimo";
    console.log(personName + sport);
  }
}
// Ya que la variable personName esta declarada dentro del boque if,
// no se puede llamar a esta fuera.
canPlay();
